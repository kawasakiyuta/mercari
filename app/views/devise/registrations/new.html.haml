-# %h2 Sign up
-# = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
-#   = render "devise/shared/error_messages", resource: resource
-#   .field
-#     = f.label :nickname
-#     = f.text_field :nickname, class: "input-default", autofocus: true, autocomplete: "nickname", placeholder: "例) メルカリ太郎"
-#   .field
-#     = f.label :email
-#     = f.email_field :email, class: "input-default", autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
-#   .field
-#     = f.label :password
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#     %br/
-#     = f.password_field :password, class: "input-default", autocomplete: "new-password", maxlength: "7"
-#   .field
-#     = f.label :password_confirmation
-#     %br/
-#     = f.password_field :password_confirmation, class: "input-default", autocomplete: "new-password", maxlength: "7"
-#   .actions
-#     = f.submit "Sign up"
-# = render "devise/shared/links"
.display
  %header.header-box-registration
    %nav.progress-bar
      %ol.clearfix
        %li.bar-name
          会員情報
          .progress-line--first--index
        %li
          電話番号認証
          .progress-line
        %li
          お届け先住所入力
          .progress-line
        %li
          支払い方法
          .progress-line
        %li
          完了
          .progress-line--fifth
  %main.contents
    %section.contents__panel
      %h2.contents-name 会員情報入力
      .contents-box
        .contents-form
          = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
            = render "devise/shared/error_messages", resource: resource
            .form-group
              %label ニックネーム
              %span.form-require 必須
              -# = f.text_field :nickname, class: "input-default", autofocus: true, autocomplete: "nickname", placeholder: "例) メルカリ太郎"
              = f.text_field :nickname, class: "input-default", autofocus: true, autocomplete: "nickname", value: "メルカリ太郎"
            .form-group
              %label メールアドレス
              %span.form-require 必須
              -# = f.email_field :email, class: "input-default", autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
              = f.email_field :email, class: "input-default", autofocus: true, autocomplete: "email", value: "gmail@gmail"
            .form-group
              %label パスワード
              %span.form-require 必須
              - if @minimum_password_length
                -# = f.password_field :password, class: "input-default", autocomplete: "new-password", placeholder: "7文字以上", maxlength: "7"
                = f.password_field :password, class: "input-default", autocomplete: "new-password", value: "12345678", maxlength: "7"
            .form-group
              %label パスワード(確認)
              %span.form-require 必須
              -# = f.password_field :password_confirmation, class: "input-default", autocomplete: "new-password", placeholder: "7文字以上", maxlength: "7"
              = f.password_field :password_confirmation, class: "input-default", autocomplete: "new-password", value: "12345678", maxlength: "7"
            .form-group
              %label 本人確認
              %p.confirmation-text
                安心・安全にご利用いただくために、お客様の本人情報の登録にご協力ください。他のお客様に公開されることはありません。
            .form-group
              %div
                %label お名前(全角)
                %span.form-require 必須
              -# = f.text_field :first_name, class: "input-default-half", autofocus: true, autocomplete: "first_name", placeholder: "例) 山田"
              = f.text_field :first_name, class: "input-default-half", autofocus: true, autocomplete: "first_name", value: "山田"
              -# = f.text_field :last_name, class: "input-default-half", autofocus: true, autocomplete: "last_name", placeholder: "例) 彩"
              = f.text_field :last_name, class: "input-default-half", autofocus: true, autocomplete: "last_name", value: "彩"
            .form-group
              %div
                %label お名前カナ(全角)
                %span.form-require 必須
              -# = f.text_field :first_name_kana, class: "input-default-half", autofocus: true, autocomplete: "first_name_kana", placeholder: "例) ヤマダ"
              = f.text_field :first_name_kana, class: "input-default-half", autofocus: true, autocomplete: "first_name_kana", value: "ヤマダ"
              -# = f.text_field :last_name_kana, class: "input-default-half", autofocus: true, autocomplete: "last_name_kana", placeholder: "例) アヤ"
              = f.text_field :last_name_kana, class: "input-default-half", autofocus: true, autocomplete: "last_name_kana", value: "アヤ"
            .form-group
              %label 生年月日
              %span.form-require 必須
              .contents-form__birthday
                .birthday-select.select-year
                  =fa_icon 'icon-arrow-bottom'
                  .birth_date_field
                    = raw sprintf(f.date_select(:birthday, use_month_numbers: true, start_year: 1930, end_year: (Time.now.year - 18), default: Date.new(1989, 1, 1), date_separator: '%s'), '年 ', '月 ') + '日'
              = f.text_field :phone_number, value: "0000-000-000", type:"hidden"
              = f.text_field :postcode, value: "000-0000", type:"hidden"
              = f.text_field :prefecture, value: "0000", type:"hidden"
              = f.text_field :city, value: "0000", type:"hidden"
              = f.text_field :block, value: "0000", type:"hidden"
              = f.text_field :wallet, value: "0000", type:"hidden"
            .contents-form__confirmation-text
              ※本人情報は正しく入力してください。会員登録後、
              修正するにはお時間を頂く場合があります。
            .form-group
              .contents-form__captcha
              ☑︎reCAPCHA(私はロボットではありません)
            .form-group
              .confirmation-text
                「次へ進む」のボタンを押すことにより、
                = link_to "利用規約", "https://www.mercari.com/jp/tos_list/", class: "blue-link"
                に
                同意したものとみなします
            = f.submit '次へ進む'
          .description
            =link_to "本人情報の登録について＞", "https://www.mercari.com/jp/help_center/article/423/", class: "blue-link"
折りたたむ







yuta_kawasaki へのメッセージ


